services:
  - type: web
    name: electricity-theft-app
    env: python
    region: oregon
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: streamlit run app.py
    runtime: python-3.10.13
